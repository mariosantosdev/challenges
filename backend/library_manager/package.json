{
  "name": "library_manager",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "yarn tsc",
    "start": "yarn build && node build/",
    "dev": "tsnd --exit-child --respawn --transpile-only src/index.ts"
  },
  "dependencies": {
    "@prisma/client": "^3.3.0",
    "cors": "^2.8.5",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.12",
    "@types/express": "^4.17.13",
    "dotenv": "^10.0.0",
    "prisma": "^3.3.0",
    "ts-node-dev": "^1.1.8"
  }
}
